/**
 * Handoff Protocol v1.5 — Context Map (Node wrapper).
 *
 * The implementation lives in ../context-map.mjs and is shared verbatim with
 * the Deno runtime and the shared test suite, guaranteeing both runtimes
 * produce equivalent results for the same fixtures.
 */

export * from "../context-map.mjs";
